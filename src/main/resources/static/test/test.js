layui.use(['form', 'upload','jquery'], function(){  //如果只加载一个模块，可以不填数组。如：layui.use('form')
  var form = layui.form //获取form模块
  ,upload = layui.upload //获取upload模块
      ,$ = layui.jquery
   ,layer = layui.layer;


   $(function() {
       layer.msg("2333333");
   });

});